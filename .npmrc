; Use hoisted node_modules structure (like npm/yarn).
; This ensures workspace packages are always accessible at the root level
; and TypeScript module resolution works correctly in all environments.
node-linker=hoisted
