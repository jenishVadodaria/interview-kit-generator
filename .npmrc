; Ensure workspace packages are always symlinked into each package's node_modules
link-workspace-packages=true

; Use the workspace lockfile
shared-workspace-lockfile=true

; Hoist packages so TypeScript can always find workspace dependencies
public-hoist-pattern[]=*
