{
  "name": "cli",
  "version": "1.0.0",
  "private": true,
  "description": "Interview Prep Kit — Batch CLI entry point",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@interview-prep-kit/pipeline": "workspace:*",
    "@interview-prep-kit/shared": "workspace:*",
    "dotenv": "^16.0.0",
    "yargs": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/yargs": "^17.0.0",
    "typescript": "^5.4.0"
  }
}
