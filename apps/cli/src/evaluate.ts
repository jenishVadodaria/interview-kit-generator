#!/usr/bin/env node
// Batch CLI — calls runKitPipeline() (same function the API uses).
// Usage: npm run evaluate -- --input cases.json --output kits.json
// Full implementation in Commit 12.
console.info('Batch CLI — full implementation coming in Commit 12 (after pipeline is complete)');
console.info('Usage: npm run evaluate -- --input cases.json --output kits.json');
